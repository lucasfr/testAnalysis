# This file is intentionally empty.
# Use RSTUDIO_PROFILE in .env to select a build profile:
#   minimal  (default) — P3M binaries, no Stan, no lunaR
#   no-stan             — P3M binaries, no Stan, with lunaR
#   full                — P3M binaries, Stan + lunaR
#
# docker-compose will automatically use Dockerfile.${RSTUDIO_PROFILE}
