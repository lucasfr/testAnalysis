IMAGE_NAME=circadiabase_docker

# RStudio auth — set to 'true' to disable password (local dev only)
DISABLE_AUTH=true

# Services to run — controls which containers start
# Options: both (default) | jupyter | rstudio
COMPOSE_PROFILES=both

# Install local Python package in editable mode (requires pyproject.toml at repo root)
INSTALL_PACKAGE=false

# RStudio build profile — controls which Dockerfile is used for the R service
# Options: minimal (default) | no-stan | full
RSTUDIO_PROFILE=minimal
